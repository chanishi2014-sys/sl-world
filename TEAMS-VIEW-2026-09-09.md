# Teams閲覧再構成・能力/特殊能力表示（2026-09-09）

## 変更ファイル

- `index.html`：Historyから現在所属への2タブ・一覧パネルを取り除き、既存「チーム」画面を閲覧入口へ変更。クラブ間関係はリーグ選択画面の折りたたみ項目として保持。
- `team-registry.js`：既存の22チーム台帳を共通ファイルへ移動。固定ID・名前・地域・旧名は維持し、`league`だけを変更可能な所属情報として公開。正式リーグ名はSL=SUPER LEAGUE、TL=TECHNICAL LEAGUE。
- `teams-view.js` / `teams-view.css`：リーグ→チーム→登録選手→基本能力/特殊能力の閲覧UI、保存データの読み取りとID索引。
- `special-abilities.js` / `special-abilities.css`：選手エディターの既存特殊能力定義と色・虹グラデーションを移動し、エディターと閲覧画面で共有。新しい特殊能力・性能・独自分類は追加していない。
- `player-editor.html`：共通チーム台帳・特殊能力定義・色を読み込み。保存時に既存の`team`に加えて`teamId`を記録。能力・特殊能力の入力値/性能は変更しない。
- `service-worker.js`：新しい共有JS/CSSをオフライン対象に追加、キャッシュ版をv17へ更新。
- `history-teams.test.cjs`：前回のHistory所属閲覧テストを、新しいTeams構成・選手詳細・共通データ・保存互換性の回帰テストへ更新。
- `test-artifacts/teams-view-protected.json`：開始時点のエンジン/LAB/戦術/共通能力表示等33ファイルのハッシュ。
- `test-artifacts/teams-league-selector.png` / `teams-player-traits.png`：隔離ブラウザのテストデータによる画面確認。実ユーザーの選手保存領域には生成・書き込みをしない。
- この文書。

## 画面と所属情報

HistoryはSLの歴史・CC・JC・国王杯・年表・伝説の既存経路を保持し、現在所属への導線はない。

TeamsはSL所属チーム/SUPER LEAGUEとTL所属チーム/TECHNICAL LEAGUEから始まり、各チームの`league`をフィルターして一覧と件数を生成する。チーム詳細では現名称・正式リーグ名・地域・登録選手を表示する。未登録チームには「選手データ未登録」。戻る操作で元のボタンへフォーカスを戻す。

`SL_TEAM_REGISTRY.byId(id).league`を変更すると通知により表示が追従する。SL⇔TL、TL⇔ELに相当するデータ変更をテストした。ELの画面・試合機能は追加していない。所属変更の自動判定・シーズン処理・永続化は将来の管理処理の役割であり、今回は実装していない。再読み込み時の初期所属は台帳データによる。

## 選手の参照

試合エンジンと同じ`sl_world_players_v01`から、同じ配列形式または`{players:[...]}`を読み取る。選手の`id`をそのまま保持し、`teamId → player IDs → player`で参照する。同名選手もIDで区別する。

既存の`teamId`があれば最優先し、名称が食い違っても名称から別チームへ付け替えない。`teamId`のない旧データだけ、台帳の確定済み名称/旧名/別表記からIDへ読み取り時に対応付ける。保存値は書き換えない。重複/欠落IDや不明な所属を勝手に生成・補完しない。

選手エディターは今後の明示保存時に`teamId`を付け、既存エンジン向けの`team`文字列も残す。チーム変更時には両方が同じ所属になる。

## 基本能力

ミート・パワー・走力・肩力・守備力・捕球を2段×3列で表示。保存された`batting`値を直接表示し、試合中の実効値・補完値に置き換えない。

判定は既存`SL_ABILITY_DISPLAY.describeAbility`（`lineup-display.js`）を呼び、色は既存`lineup-display.css`の`.ability-rank`/`.rank-*`をそのまま再利用。この2ファイルは未変更。

G灰 #b6bec8 / F青 #71b7ff / E緑 #76d79b / D黄 #f1e569 / C橙 #ffb66b / B赤 #ff8585 / A桃 #f7a5d3 / S金 #e9c35d。DとSは異なる色。未設定・範囲外は共通定義に従い表示し、架空のランクを割り当てない。

## 特殊能力

保存された`specials`を折りたたみ「特殊能力・一覧を見る」から表示。名前の文字や○/×から推測せず、既存`SPECIALS`の明示的な`[name,color,registration]`定義へ完全一致で問い合わせる。分類未知は「カテゴリ未登録」。

青/緑/赤/金は既存パレットの独立枠、虹は既存7色グラデーションを枠として再利用する。赤は「マイナス系」、金は「上位」、虹は「最上位・特別」のラベルも表示。能力Sの金色は能力カード、金特殊能力は特殊能力バッジに分離している。

## 検証結果

- `node history-teams.test.cjs`：PASS。元の22チーム定義・全特殊能力カテゴリ・歴史本文/データが一致。対象外33ファイルも一致。
- データ検査：SL/TL/ELへの所属変更、選手IDの型/重複、旧名参照、明示teamIdの優先、未登録データ、読み込みエラー。
- ブラウザ：全22チームの一覧/詳細/戻る、旧6アーカイブ、同名選手の区別、未登録選手表示、リーグ変更通知。
- 基本能力：6値とランク、8ランクの実際のCSS色、DとSの相違、幅320/390/1280pxで2段×3列・横はみ出しなし。
- 特殊能力：初期は閉じる、5分類の枠/ラベル・金枠・虹グラデーション、未知カテゴリを推測しない。
- 保存互換性：閲覧/再読み込みでlocalStorage不変。エディターの明示保存とSL→TLへの所属変更でも能力値・特殊能力の配列は一致しteamIdだけ整合させる。
- オフライン：新共有リソースをキャッシュした後のTeamsとエディターの読み込みが成功。
- `node lineup-display.test.cjs`：PASS。9能力の776整数値・全境界・未設定/対象外・元のオーダーUI/二刀流表示。
- JS構文と`git diff --check`：PASS。

試合エンジン・戦術AI・LAB・試合結果・歴史データ本体・昇降格ロジックは未変更。公開/デプロイは未実施。
