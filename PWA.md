# SL WORLD PWA

GitHub Pagesの `/sl-world/` 配下に、通常のページと一緒に配信します。
manifestの `id`・`start_url`・`scope` は `./`。manifest自身の場所を基準に解決されます。
ローカルではHTTPのlocalhostで配信してください。file://ではservice workerは動作しません。

## iPhone

公開反映後、Safariで `/sl-world/` を開き、共有 → ホーム画面に追加。
「Webアプリとして開く」が表示される環境では有効にします。
ホーム画面のSL WORLDアイコンから起動します。実機での確認は別途必要です。
4ページともmanifestとiOS用metaを設定済み。同じscope内の通常リンク遷移を利用し、
新しいウィンドウへ開くリンクは避けてください。今回、新メニューは追加していません。

## 更新・キャッシュ

- HTML・登録スクリプト・manifest・アイコンはネットワーク優先。HTTPキャッシュも迂回します。
- 通信失敗・サーバー5xx時だけ、最後に取得できた同じURLのキャッシュを返します。
- 初回オンライン時に4ページとPWA用資材をキャッシュします。
- service workerは `updateViaCache: none` で登録し、アプリ復帰時にも更新を確認します。
- 新workerは即時有効化しますが、ページを強制リロードしません。入力中のフォームを保持します。
- workerや事前キャッシュ一覧を変更するときは `service-worker.js` の `VERSION` を上げてください。
- 通常のHTML更新はVERSION変更なしでも、次回オンライン読込で反映されます。
- オフライン中は以前のページが表示されます。公開更新後はオンラインで再読み込みしてください。
- キャッシュ削除はこのアプリ専用の古いレスポンスキャッシュだけが対象です。
- PWA用コードはlocalStorage・IndexedDBに触れません。移行・初期化処理はありません。
- Safariとホーム画面アプリの保存領域の見え方はiOS環境に依存するため、実機でも確認してください。
  この実装で保存データの自動移行は行いません。

## 仮アイコンの差し替え

現在は単純な「SL / WORLD」の仮マークです。正式デザインではありません。
同じファイル名・サイズのPNGへ差し替えれば参照先の変更は不要です。

- `icons/apple-touch-icon.png` — 180×180、iPhone用
- `icons/icon-192.png` — 192×192
- `icons/icon-512.png` — 512×512

iOSでインストール済みアイコンが残る場合は実機の表示を確認してください。
データ保護のため、再インストールや保存領域削除を自動実行する処理はありません。
