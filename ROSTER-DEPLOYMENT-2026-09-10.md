# 所属選手一覧・公開未反映の追加調査（2026-09-10）

## 確定した原因

前回修正した画面は、今回指定された `index.html → renderTeams → SL_TEAMS_VIEW.render → デビルスターズ → 所属選手` と同一。
ただし、公開中の `https://chanishi2014-sys.github.io/sl-world/teams-view.js` を直接取得すると、名前だけの旧描画処理だった。
GitHubのmainを `git ls-remote` で照合すると `3880bdc449885bf3803eab1bc513d86379f23e43`。
ローカルの前回修正は `cb4ba8e` で、公開元より1コミット先にある。前回の修正はGitHubへ未送信だった。
前回はローカルの別チーム・テスト用ロスターだけで確認し、公開反映と実際のデビルスターズを確認しなかった。ここが完了確認の不足。
Service Workerはオンライン時network-firstであり、今回の公開旧コードをキャッシュだけの問題とは扱わない。

## 今回の変更

`teams-view.css` の対象カードを、名前を上段、6能力を下段の横一列に配置する形へ調整。
上下余白8px、段間4px。能力ランクの直下に10pxの実数値を置く。
能力データは `teams-view.js` の既存登録選手 `p.batting`、ランクは選手詳細と共通の `SL_ABILITY_DISPLAY.describeAbility`。
ミート→パワー→走力→肩力→守備力→捕球の順と既存色を維持。能力名は読み上げ用のみで、画面上は非表示。

## 検証と未確認

- 能力判定の既存テスト：776整数値・境界・未設定値の検証を通過。
- `git diff --check` 通過。
- 公開版で「チーム・選手 → デビルスターズ → 所属選手」まで実際に開いた。
- 接続可能なCodexブラウザでは「選手データ未登録」。保存は端末ごとのlocalStorageであり、ユーザーのスマホ内選手はここに存在しない。
- 檜山・明日川・御子柴をダミーで作成せず、実データ表示の最終確認は保留。実機からJSON EXPORTした選手データが必要。
- 公開への反映と、公開版で実選手を使った最終確認が完了するまでは、修正完了とはしない。
