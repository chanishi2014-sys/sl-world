# 二塁責任継承・BASE/HOME到着後HOLD Completion Report

2026-09-15夜。直前の緊急修正（`049ccd8`）を基準に、二塁責任とBASE/HOME COVERの移動記録だけを修正しました。公開反映・実機iPhoneでの最終再観戦は未実施です。

## 変更ファイル

- `engine-fielding.js` / `engine006-fielding.js`：二塁の次プレー入力・後継者評価、BASE/HOME COVERの実到着時刻・HOLD記録。両ファイルは同一。
- `service-worker.js`：キャッシュv26→v27。オフライン用の旧コード残存への対処。
- `second-cover-hold.test.cjs`：TEST-1〜5、受球時の再始動、保護対象コード比較。
- `test-artifacts/second-cover-*.json`：今回の証跡。以前の検証証跡は別途保持。

## 1. 二塁責任が継承されなかった原因

二つの経路を分けて確認しました。

**必要性入力の不足**：既存のbaseNeedsは一塁・二塁の既存走者に基づく二塁責任を判断していましたが、打者走者が先に一塁へ到達し得ることや、二塁への返球・中継予定が入力されていない経路がありました。この場合、二塁はNOT_CURRENTLY_REQUIREDのままで、SS/2BはHOLDになります。

**移動記録の不整合**：一塁走者ありの代表例では、旧コードでもSS/2Bへの割当自体は存在しました。ただし、内部で到着済みでも移動区間の終了時刻が捕球時刻まで延長され、表示上の到着が遅れていました。「二塁カバー機能が全く存在しなかった」という原因ではありません。

実機映像のseedがないため、報告された場面すべてがどちらの経路かは断定していません。

## 2. 二塁責任必要性の判定

既存のREQUIRED/POTENTIAL/NOT_CURRENTLY_REQUIREDを維持し、二塁に限って次の入力を追加しました。

- 現在の捕球予測で空中捕球が見込めず、処理ETAが打者走者の一塁到達ETA以降：二塁進出へのPOTENTIAL。
- 拾い上げ後、打者走者が一塁へ到達し得る時刻になった場合：POTENTIAL。
- 既存の二塁への返球・中継先が候補になった場合：POTENTIAL。

既存走者、フォース、帰塁、タッチアップ、アウト数による判定は維持しています。走者も次プレーもない空いた二塁には新しいカバーを発生させません。一塁・三塁・本塁の必要性判定は変更していません。

## 3. 後継者選択

現在のPRIMARY/SECONDARY、既に割り当てた重要責任の担当者を除外し、残る候補の現在位置からの到達時間、元の守備位置から二塁への近さ、既存二塁責任の継続性を比較します。

元位置による評価は二塁だけに追加しました。「2Bが離れたらSS」「SSが離れたら2B」という分岐は追加していません。現在位置を変えた検査では別の適任者が選ばれることも確認しました。Pによる一塁・三塁継承の一般化は行っていません。

## 4. TEST-1

浅い右方向、一塁走者あり。2Bが打球処理者となり、SSが二塁POTENTIAL責任を担当。**約2.283秒で二塁に到着**し、捕球前の2.6秒時点でも二塁を保持します。

## 5. TEST-2

浅い左方向、一塁走者あり。SSが打球処理者となり、2Bが二塁POTENTIAL責任を担当。**約2.261秒で二塁に到着**し、捕球前に保持します。

## 6. TEST-3

左右それぞれの浅い打球で、走者なし・空中捕球可能・二塁への次プレーなしを検査。二塁はNOT_CURRENTLY_REQUIREDのまま、担当者は割り当てません。

別検査で、打者走者の次塁進出／二塁への返球候補を渡した場合だけ二塁POTENTIALが成立することを確認しました。

## 7. Cの足踏み原因

Cの内部位置は通常速度で本塁へ到着していました。しかし、毎シミュレーション刻みで同じbaseCover区間のendを更新し、到着後もその区間を延長していました。

旧fixtureでは、32座標単位の移動区間が約0.313秒〜2.88秒となっていました。再生側はその区間全体を移動と解釈するため、歩行が長引いていました。落下時刻への明示的な速度同期処理ではなく、移動区間の結合処理の不具合です。

## 8. 1Bの足踏み原因

Cと同じ結合処理です。約18.38座標単位の一塁への移動が、内部で到着済みでも捕球時刻まで引き延ばされていました。再割当時には同一座標間の短い移動区間も生成されていました。

目標となるベース座標自体の往復は、今回の固定fixtureでは確認していません。到着後の微小な再指定についても停止状態を保持するようにしました。

## 9. HOLD安定化

BASE/HOME COVERだけに到着を記録する処理を追加しました。

1. 既存の反応時間・移動速度・経路補正で通常移動。
2. 最後の移動刻みで、実際に到着した時刻を算出。
3. そこで移動区間を閉じ、`movementState: HOLD`の停止記録を追加。
4. BASE/HOMEの責任とbase番号は保持。
5. 到着後、同じ目的地の微小な再指定（0.05座標単位以内）では移動を再開しない。
6. 受球等の実際の新しい役割・目的地には通常どおり移動。

再生アニメーションだけを停止してはいません。エンジンの移動時系列を修正し、既存の再生側がその時系列から歩行停止を判断します。再生コード、PRIMARY/SECONDARYの移動計算、能力速度は変更していません。

## 10. TEST-4/5

| 担当 | 到着時刻 | 到着後 |
|---|---:|---|
| C→本塁 | 約1.110秒 | HOLD、位置一定、歩行状態false |
| 1B→一塁 | 約0.771秒 | HOLD、位置一定、歩行状態false |

滞空時間3秒と6秒の両条件で到着時刻が一致しました。到着後から捕球直前まで0.01秒刻みで、位置と再生用の`pose.running`を検査しました。

微小な目的地再指定ではHOLDを継続し、その後の実際のreceive指示で移動を再開することも確認しました。

## 11. emergency-fielding回帰

`emergency-fielding.test.cjs`の全検査を再実行し、通過しました。

- 通常CF/RF/LFフライ。
- 2B後方フライ、浅いCF前・RF前。
- 旧P追跡／C三塁カバー再現打球。
- 外野手が間に合わない深い打球の拾い上げ（強制捕球なし）。
- P/Cのバント、P/C周辺ポップ。
- 5-4-3の2アウト。

`final-2026-09-15.test.cjs`も全11検査が通過。2B intercept、runner jitter、FC、DP表示、RF→一塁のセーフ／アウト競争、強制送りバントを維持しました。

盗塁回帰は直接300条件、LAB27、CUSTOM5、通常試合13、H&R1が通過。二盗時の2B捕球・タッグ仕様を維持しています。

## 12. 通常C対C回帰

`emergency-night-C-C-0`〜`5`の6試合を通常の試合進行で実行し、すべて完走しました。打球・乱数をfixtureへ差し替えていません。

外野方向フライの実移動ログ163件を検査。試合別の対象数は32 / 27 / 25 / 24 / 27 / 28件です。別の保護対象回帰でもC対C、S対G、G対Sが完走しました。

## 13. 全員追跡の再発確認

通常の外野手処理機会がある76件で、所有確定後の内野手追跡を検査し、再発なし。浅い打球への正常な内野手の反応は残しています。

NATURAL FIELDING CANDIDATE、intercept探索、PRIMARY/SECONDARYの選定部分は、基準コミットとコード内容が一致することを検査しました。

## 14. P/C異常移動の再発確認

6試合の対象フライで、Pの外野追跡／打球後方BACKUPは0件、Cの不必要な他塁カバーも0件でした。既存のP/C局所打球処理は成立しています。

## 15. 未解決事項・確認範囲

- 確認したfixtureと回帰試合に既知の失敗は残っていません。
- 実機iPhoneでの最終再観戦と公開反映は未実施です。実機の個別場面と完全に一致するseedでの検査ではありません。
- Pによる一塁・三塁継承の一般化、追跡AI再設計、能力・得点・各種戦術率の調整は行っていません。
- 今回の移動時系列修正はBASE/HOME COVER限定です。

## 証跡・実行

- `node second-cover-hold.test.cjs` → `test-artifacts/second-cover-hold.json`
- `node emergency-fielding.test.cjs` → `test-artifacts/second-cover-emergency-movement.json`
- `node final-2026-09-15.test.cjs` → `test-artifacts/second-cover-preserve.json`
- `node steal-cover-regression.test.cjs` → `test-artifacts/second-cover-steal.json`
- `node emergency-compare.cjs working` → `test-artifacts/second-cover-comparison.json`
- `node final-2026-09-15.browser.test.cjs` → `test-artifacts/second-cover-browser.json`

通常入口／ENGINE006入口の観戦・完走・表示回帰も確認。構文、ミラー一致、diffチェックも実施しました。
