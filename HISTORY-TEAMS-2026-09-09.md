# SL / TL 現行所属チーム・History追加（2026-09-09）

## 今回の変更ファイル

- `index.html`：共通チーム台帳、HistoryのSL/TL所属一覧・詳細、現在チームを示す表示の整理。
- `history-teams.test.cjs`：正規22チーム、旧名参照、履歴非破壊、画面操作、保存互換性の回帰検査。
- `test-artifacts/team-history-protected.json`：今回開始時のエンジン/LAB/戦術等33ファイルのSHA-256。前の依頼で行った変更を含む作業開始時点から追加変更がないことを確認する。
- `test-artifacts/history-tl-clubs.png`、`test-artifacts/history-tl-detail.png`：スマホ画面検証記録。
- この文書。

## 正規データ

指示本文のSL 12チーム・TL 10チームを、その順序・名称・地域で登録。画像は旧名資料として使用。全チームが同じ構造を持つ。

`id / currentName / league / region / formerName / aliases`

- 固定IDは`club-001`～`club-022`。名称や所属リーグをIDに含めず、今後の変更でも同じチームを参照できる。
- 旧名称は同じチームの参照用情報。旧チームの別レコードを生成しない。
- 東京アスレチックスは画像の「日体荏原」と既存コードの「日本体育大荏原」を同じIDへ対応させる。
- 資料の「HG学院」と既存歴史にある「HG学園」等、別表記か確定できないものを推測で結び付けない。
- 不明な名称・空値は未解決のまま返す。既存情報の生成・全置換・保存データ移行は行わない。
- 台帳と一覧はindex.html内に置き、新しい外部リソースやPWAキャッシュ構造の変更は不要。

## History UI

既存タブへ「SL 所属チーム」「TL 所属チーム」を追加。SLの歴史・CC・JC・国王杯・年表・伝説の既存経路を維持。

一覧には現名称・リーグ・地域、詳細にはチーム名・所属リーグ・地域と資料の旧名を表示。チームIDで詳細を開く。一覧に戻ると元のボタンへフォーカスが戻る。TLの架空の監督・選手・順位・成績などは表示しない。

現在のクラブカード・クラブ間関係、SL/JCのチーム単位の通算集計表示は現名称を使用。通算回数は既存データの計算を維持し、新しい成績を加えない。

## 未変更の歴史・動作

- 大会別の優勝者・結果、CC/JC/国王杯の元データ、年表、伝説、過去記事の見出し・本文・移籍記録は当時の名称を保持。
- 既存大会処理のチーム名称キー、監督参照、ポイント、Big6、保存済み所属・移籍・関係性を維持。表示名だけを台帳で解決する。
- 第34回など既存大会の入力・集計処理を現名称へ一括移行しない。現行所属台帳と過去の大会キーは別の役割。
- TLの日程・対戦・順位・シーズン・昇降格・大会参加・AI試合・選手自動生成は未実装のまま。
- 今回、試合エンジン・戦術AI・LABには追加変更なし。前の作業の未コミット変更も保持。

## 確認

`node history-teams.test.cjs`（Playwrightは既存のバンドル依存を使用）:

- 22チームの名称・地域・リーグが指示と完全一致、ID一意、正規データ以外の架空フィールドなし。
- 旧名/別表記から同じIDを参照。不明な名称を推測で解決しない。
- 既存のチーム・監督・ポイント・歴史データ定義と全歴史記事のソースが変更前revisionと一致。
- エンジン/LAB/戦術等33ファイルが今回開始時と同じハッシュ。
- SL 12件・TL 10件の一覧、全22詳細、戻る操作・フォーカス、既存6アーカイブ、現在チーム表示。
- 幅320/390/1280pxの一覧・詳細に横方向のはみ出しなし。
- 保存済み選手所属・移籍・関係性・大会・メモが操作後・再読み込み後も同じ。
- ページ例外なし。inline JavaScript構文検査、`git diff --check`。

公開・デプロイは行っていない。
