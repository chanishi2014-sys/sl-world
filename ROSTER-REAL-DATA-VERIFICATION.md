# 実エクスポートによる所属選手一覧の接続確認

対象：`SL_WORLD_PLAYERS_2026-09-10.json`（SL_WORLD_PLAYER_DB v0.2、18名）。

## 接続・変更

- 既存選手エディターの「JSON読込」から、添付された実データ18名をローカル確認ブラウザへ読み込んだ。
- 一覧と詳細は同じ保存キー `sl_world_players_v01` と同じ選手IDを使用。
- `teams-view.js` の `basicAbilities` に、登録値・既存ランク判定を共通化。一覧と詳細の双方がこの関数を使用。
- 所属はteamId、旧形式はteam名の既存レジストリ照合。選手名でJOINしない。添付の表記「桧山」をそのまま保持し、「檜山」という別名の仮選手は作成していない。
- デビルスターズ9名、明道仁球会9名。読み込み除外0名。JSONラッパー形式・players配列形式とも18名を認識。
- 添付JSONを固定能力表として製品へ埋め込む処理は追加しない。選手編集後も保存済みの最新データを参照する。

## 実画面で確認した値

並びはミート・パワー・走力・肩力・守備力・捕球。画面に能力名ラベルは表示しない。

| 選手 | 実数値 | 既存判定によるランク |
|---|---|---|
| 桧山 | 10 / 186 / 20 / 18 / 19 / 19 | S / A / S / A / A / A |
| 明日川 | 9 / 178 / 20 / 18 / 19 / 20 | A / A / S / A / A / S |
| 御子柴 | 10 / 189 / 19 / 20 / 20 / 20 | S / A / A / S / S / S |
| クラウド | 10 / 200 / 17 / 19 / 17 / 18 | S / S / B / A / B / A |

- 「チーム・選手 → SL所属チーム → デビルスターズ → 所属選手」を表示し、9名全員の一覧と詳細の各6能力を照合、一致。
- 共通関数への変更後も再読込し、9名全員を再照合、一致。
- 360px / 320px幅で、能力6列が同じY座標・行内横はみ出しなし・能力名の可視テキストなしを確認。
- `node roster-real-data.test.cjs`：実JSON18名・108能力・所属・ID・入力形式・原ファイル非変更、通過。
- `node lineup-display.test.cjs`：既存776整数値・境界など、通過。
- `git diff --check`：通過。

## 公開状況

今回確認したのは、修正コードを配信するlocalhostの実アプリ画面と、添付の実データ。
公開中GitHub Pagesのteams-view.jsは再取得時点でも名前だけの旧版だった。
前回の未送信コミットと今回の変更が公開元へ反映されるまでは、スマートフォンの公開版が更新済みとは扱わない。
実データが入っている実機では再インポートによる置換は不要。コードの公開反映後、オンラインで再読み込みする。
別ブラウザの「選手データ未登録」は保存領域が空の場合に表示される。添付JSONの選手名の表記差や所属照合の失敗ではなかった。
