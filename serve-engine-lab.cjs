// Static, localhost-only preview; no database writes or publishing.
const http=require('http'),fs=require('fs'),path=require('path');const root=__dirname,port=Number(process.env.SL_LAB_PORT||4173);
http.createServer((req,res)=>{let pathname;try{pathname=decodeURIComponent(new URL(req.url,'http://localhost').pathname);}catch{res.writeHead(400).end();return;}const file=path.resolve(root,'.'+(pathname==='/'?'/engine-lab.html':pathname));const ext=path.extname(file),mime={'.html':'text/html','.js':'text/javascript','.css':'text/css','.png':'image/png','.webmanifest':'application/manifest+json'}[ext];if(!file.startsWith(root+path.sep)||!mime){res.writeHead(403).end();return;}fs.readFile(file,(e,b)=>{if(e)res.writeHead(404).end();else{res.setHeader('Content-Type',mime+'; charset=utf-8');res.setHeader('Cache-Control','no-store');res.end(b);}});}).listen(port,'127.0.0.1',()=>console.log(`SL WORLD ENGINE LAB: http://127.0.0.1:${port}/engine-lab.html`));
