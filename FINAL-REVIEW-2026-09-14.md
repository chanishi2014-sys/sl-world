# SL WORLD 2026-09-14 実装・検証報告

対象ENGINE: `process-engine-20260914-v1`。PWAキャッシュ: `v22`。

結果率を合わせるグローバル係数は変更していません。通常打球の生成・飛距離投影処理は変更前との同一性テストを通過しました。ローカル作業ツリーへの実装です。

## 1. 変更したファイル

| ファイル | 内容 |
|---|---|
| `engine-fielding.js`, `engine006-fielding.js` | 固定守備境界の撤去、空中打球の追球再評価、守備役割と走者の読みの整合、盗塁の距離・加速・受領〜タグ時計、共通handling取得 |
| `engine-tactics.js`, `engine006-tactics.js` | バント品質を小飛球リスク・方向精度へ接続、工程ログ |
| `match-viewer.js`, `engine006-viewer.js` | 停止・リード・移動中を含む走者モデルのグローブ非表示 |
| `engine006.js` | ENGINE版の更新のみ。通常の能力係数は変更なし |
| `engine-lab-core.js` | 明示的なイベント保持制御 |
| `engine-lab-worker.js` | 逐次集約、薄い全試行レコード、限定した代表工程、条件送信後の解放 |
| `engine-lab-presentation.js` | 代表抽出の上限、代表工程のサイズ制限、集約済み理由の読み戻し |
| `engine-lab-ui.js` | 条件単位の全件JSON生成、生成中表示・エラー処理 |
| `engine-lab.html` | 新診断LABへの入口 |
| `engine-diagnostic.html`, `engine-diagnostic-ui.js` | 診断A〜E、能力・球数CUSTOM入力、工程表示・JSON保存 |
| `engine-diagnostic-core.js`, `engine-diagnostic-worker.js` | 同一条件実験、工程測定、オンライン集計、各条件最大12代表seed |
| `service-worker.js` | 新LABのオフライン資産登録・キャッシュ更新 |
| `process-regression.test.cjs`, `engine-lab-regression.test.cjs` | 再評価原則に合う守備期待値とENGINE版の更新 |
| `final-2026-09-14.test.cjs`, `final-2026-09-14.browser.test.cjs`, `final-lab-storage.test.cjs`, `final-lab-stress.browser.test.cjs` | 新工程・実ブラウザー・集約保存・45,000試行の回帰検証 |

証跡は `test-artifacts/final-0914-*`、現行回帰結果は `test-artifacts/night-regression.json` と `test-artifacts/process-regression.json` に保存しています。

## 2. 守備追球ロジック

旧コードには、内野手を本塁から255座標単位以内に制限する捕球候補判定がありました。これを撤去しました。単に範囲を広げるだけでなく、フライ／ライナーは40ms刻みで位置を進め、120msごとに担当を再評価します。

評価対象は現在位置、移動方向、予測落下点・時刻、残り時間、移動速度、守備による経路負担、方向転換の予測遅延、本人と他選手の到達予測です。捕球は実際のボール位置・高さと選手位置が届いた時刻から決まります。

PRIMARYは到達見込みの改善が明確なときに交代します。細かな役割の往復を抑える120msの差を設けています。SECONDARYは捕球可能性が残り、優位選手との到達差が0.6秒以内の最大1人です。PRIMARYと合わせて追球は最大2人です。

それ以外はCUTOFF / BACKUP / BASE COVER / HOLDを担い、その移動も次回の現在位置として扱います。追球履歴を実際の描画トラックへ渡すため、担当決定と見た目が別々に進みません。走者のフライ判断も現在追球している選手を参照し、追っていない投手などを捕球候補として扱う不整合を解消しました。

ゴロの送球先選択、ゲッツー、中継・ベースカバーの既存処理は維持しています。固定境界撤去によりゴロの捕球候補も同じ到達条件に従います。

## 3. 盗塁の実行処理

現行コードにも盗塁特性によるスタート時間差は存在しました。しかし旧式の「走行時間＋別枠の加速時間」では、実距離を走り切った時間と加速の関係を追えませんでした。

盗塁専用の走行を以下に分解しました。

- 90フィート＝27.432mからリード3.2mを差し引いた走行距離
- 走力から得る最高速度、加速度
- 最高速度へ到達するまでの加速時間・距離
- 残距離の走行時間。短距離なら加速区間だけで到達
- 特性による動作認識・スタート反応と加速工程の差

加速は走行時間へ積分し、二重加算しません。守備側は投手動作・投球飛行・捕手受領・持ち替え・送球モーション・リリース・飛行・捕球・タグを同じ時計で記録します。捕手の従来の受領後時間は持ち替えとモーションへ分解し、肩による送球速度変換は監査のうえ維持しました。

実行のSAFE判定は `runnerETA <= tagAt` のみです。戦術選択用のchanceをSAFE抽選に流用していません。通常の走力係数は変更していません。

同一乱数5,000回ずつ、走力3×肩3×特性3＝135,000競争で、全条件の到達時間が ○ < なし < × でした。C走者・C肩ではSAFE率49.58% / 42.20% / 22.20%、平均到達3.735 / 3.767 / 3.869秒でした。これは時間競争から出た測定値で、目標率として設定した数値ではありません。

## 4. バントの実行処理

既存のバント特性による接触精度・打球品質・速度の差を維持し、打球品質を小飛球リスクと左右の配置精度にも接続しました。良い品質ほど方向のばらつきが小さくなり、バット上で浮かせるリスクも減ります。その打球を通常の守備処理・走者到達競争へ渡します。

各5,000打席の強制送りバント検証:

| 特性 | 平均打球品質 | 犠打成立 / 5,000 |
|---|---:|---:|
| ○ | 0.6185 | 2,370（47.40%） |
| なし | 0.4986 | 2,097（41.94%） |
| × | 0.3795 | 1,918（38.36%） |

戦術を選ぶ頻度と実行工程を分離して測定しています。犠打成立率への直接加算、影走り・バント職人の本格調整はしていません。

## 5. LAB高負荷問題

既存のcompact化後も長い理由ログが全試行に残り、完了時に集約、条件受け渡し、表示用解析、全件pretty JSON生成が重なる構造でした。また条件受け渡し後にもWorker側で大きな集約を保持していました。これらはコード上で確認したメモリ増加要因です。過去の白画面そのものの例外記録はないため、単一原因の断定はしていません。

対策:

1. 試行終了ごとに集約し、長い理由は条件集計にのみ保持。
2. 全件フィルター・集計分母・seed再現のため、全試行は薄いレコードとして保持。
3. 各条件の最初の12試行、各最大3判断だけ代表工程を保持。共有形式にもサイズ上限。
4. 条件送信後、Workerの試行配列と大きな集計を解放。
5. 表示用の代表seedも最大60に制限。
6. 全件JSONは条件単位に生成し、条件間でUIへ制御を返してBlobへまとめる。巨大なpretty文字列を一度に作らない。
7. JSON読み戻しで集計済み理由を失わないことを検査。

通常LAB（盗塁特性×捕手肩）の実ブラウザー検証は、9条件×5,000＝45,000試行 → 100% → 結果表示 → IndexedDB保存 → 全件JSONダウンロード → JSON読み戻しまで成功しました。

- 計算時間: 610.124秒
- 全件JSON: 41,828,874バイト
- 45,000試行進捗時の画面側JSヒープ: 39,082,038バイト
- ブラウザーpageerror: 0
- 読み戻し後も45,000試行と各条件の理由集計を保持

上のヒープは進捗時点の画面側サンプルで、Workerを含む全プロセスの最大メモリではありません。初回の旧待機テストは15分上限で完了未確認となったため、上記は進捗計測付き専用テストで最新処理を再検証した結果です。

## 6. 追加したLAB

入口は従来のENGINE LAB上部「プロセス診断 LAB-A〜E」です。

| LAB | 既定比較 | 固定・実験条件 |
|---|---|---|
| A 一塁競争 | 走力S20 / C14 / G2 | 同一ゴロ・指定守備者・同一位置。守備／肩をS・C・Gから選択可 |
| B 守備力 | S20 / B17 / C14 / G2 | 同一指定守備者、走者C14、肩・捕球固定。17はB |
| C 打球品質 | S195 / A184 / B172 / B162 / C142 | ミートC7、投球品質0.5、その他固定 |
| D 疲労 | S195 / C142 / G20 × 0 / 80 / 140球 | 同じseedの疲労なし投球との比較。現在の継投処理も呼び出す |
| E 浅いポテン | SS / 2B / 3B / 1B後方、浅いCF / LF / RF | 無走者、同一能力の浅いフライ |

数値比較・開始球数はCUSTOM入力可能です。A/Bは、指定した守備者が同一打球を処理する隔離実験です。他選手が先に処理する通常の担当選択を混ぜず、共有の到達・handling・送球判断を使用します。Aの指定守備者にRFも用意し、外野→一塁の研究に使える基盤にしました。

## 7. 保存される主要ログ

- **A/B**: 入力能力、打球種類・速度、捕球位置、指定守備者、reaction、pursuitStart、route、movementSpeed、fieldArrival、catchability、pickupStart/Complete、handling、throwInitiation、releaseTime、throwTravelTime、ballETA、runnerStart、runnerETA、margin、実際の送球判断・送球・走者アクション・finalResult。
- **C**: contactQuality、qualityBand、exitVelocity、launchAngle、GROUND/LINER/FLY、distance、landingPoint、hangTime、HR軌道候補、XBH軌道候補。ゴロのdistanceは停止位置まで、空中打球は落下位置まで。候補は結果分類ではなく、定義もJSONへ記録。
- **D**: pitchCount、stamina、fatigue、fatigueThreshold、疲労なしのrawVelocity/Control/PitchQuality、実効値、投球判断・接触・投球結果、継投判断と理由・評価球数。投球を観測してから管理判断を実行。
- **E**: 各守備者の入力能力・初期位置、各再評価時点の現在位置・方向、落下予測・残り時間、打球速度、到達予測、捕球可能性、assignedRole、roleChangeTime、reasonForRoleChange、処理者、catch/drop/bounce、最終補助役割。

A/Bのmarginは `runnerETA - ballETA`。正ならボールが先着します。実際の失策や送球見送りは別途finalResultとactualThrowで確認します。全LABでconfig・condition・seedを保存します。

## 8. 自動テスト結果

- `final-2026-09-14.test.cjs`: A〜Eの固定条件・能力工程、135,000盗塁競争、15,000バント打席、PRIMARY交代と最大2人、RF→一塁のOUT/SAFE境界、JSON保存形式。
- `night-regression.test.cjs`: 合格。内野フライ、浅い外野、タッチアップ、帰塁アピール、第三アウト、二遊間カバー、三塁線バックアップ、一塁カバー、FC分類、通常打球生成の変更なし。
- `process-regression.test.cjs --quick`: 合格。712イベントの検査、10試合、ゲッツーの工程順序、モード間再現、CUSTOMの保存seed再現等。
- `engine-lab-regression.test.cjs`: 合格。763イベント、既存シナリオ、再現、選択・バッチ、チームコピー不変。
- `final-lab-storage.test.cjs`: 合格。実Workerの逐次集計が全詳細集計と完全一致。JSON読み戻し後も集計一致。代表上限。
- `final-2026-09-14.browser.test.cjs`: A〜Eの画面・JSONダウンロードと解析、390px幅、LAB-Dの45,000試行。Dの測定時間227.391秒、JSON123,113バイト。最新ログ追加後の小規模再確認も合格。
- `final-lab-stress.browser.test.cjs`: 通常LAB45,000試行、表示、IndexedDB、全件JSON保存・読み戻し、pageerrorなし。

旧`daily-2026-09-11.test.cjs`は変更前から旧形式のforceTrace参照で失敗し、今回の合格対象には含めていません。旧SOL検証の26,000回の重複シミュレーションは途中で止め、新しい集約一致テストと実ブラウザー45,000試行の保存検査で今回の経路を検証しました。全歴史テスト一括通過を主張するものではありません。

## 9. 重点回帰

SS後方・2B後方・両コーナー後方・浅い外野7地点で役割履歴を確認。PRIMARY交代後の補助役割、同時追球最大2人を検査しました。現在良好なゲッツー・中継・カバーの既存検査は維持しています。

走者描画は停止時にもrunner役割を付け、runnerにはグローブを描きません。三塁まで占有した描画用ゲームで、描画前後の試合状態が不変であることを実ブラウザーで検査し、スクリーンショットを保存しました。

FCは完成した守備選択・送球・アウト対象に基づく既存分類を維持。新たな結果先行分類は加えていません。

RF→一塁は、走力20の打者、同じ方向0.6rad・右翼手肩20で、40m/sの打球では送球到達約4.037秒に対して走者3.86秒でSAFE、45m/sでは送球約3.726秒でOUTを確認しました。俊足の特例保護や頻度調整はありません。

## 10. 未確認・研究継続

- 人が複数の実戦を観戦して判断する「自然さ」の最終確認。FCの利用者実機での新規該当場面確認も継続対象です。
- 過去の白画面を引き起こした単一の例外・クラッシュ原因。今回は高負荷経路の成功とメモリ保持構造の改善を確認しました。
- 盗塁の物理パラメーターの実測データによる校正。S走者×G肩などはまだほぼ成功、G走者×強肩はほぼ失敗になり得ます。率の直接補正で飽和を消してはいません。
- 通常の打者走者には独立した加速区間モデルがありません。A/BのrunnerAccelerationはその事実を明記し、存在しない時間を作っていません。今回は通常走力係数の調整対象外です。
- A/Bのhandlingは既存モデルの粒度で記録します。実際の送球モーションを更に細かく分離する研究は今後の課題です。
- パワー・ミート・守備力・スタミナ・打球分布・HR/長打/得点/ライトゴロ頻度、方向系能力は数値調整していません。診断後の研究対象です。

追補: 二塁盗塁の二塁手カバー修正とv2の検証は [STEAL-COVER-2026-09-14.md](STEAL-COVER-2026-09-14.md) を参照してください。本報告のv1高負荷検証は当時の証跡として保持しています。
