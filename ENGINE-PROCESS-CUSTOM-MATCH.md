# 過程ベースの試合エンジンと CUSTOM MATCH TEST

初回改修のエンジン識別子: `process-engine-20260912-v1`

夜の限定回帰修正は [NIGHT-REGRESSION-20260912.md](NIGHT-REGRESSION-20260912.md) を参照。以下の初回検証値は日中の版の記録。

## 実装

打球生成は方向・初速・打ち出し角・高さ・軌道を生成する。生成時点では `battedResult` は未確定。守備の到達、捕球、送球、走者の到達からプレーを解決し、終了した履歴から記録を付ける。旧 `resolveBattedBall` の結果文字列を返す契約は廃止した。結果を指定して実プレーを作る呼び出しは使用しない。

主な実装箇所:

- `engine006.js` / `engine-test.html` 内コア: 打球生成、プレー解決、最終記録、乱数状態の保存。
- `engine006-fielding.js` / `engine-fielding.js`: 連続軌道、到達計算、守備役割、走塁の再評価、捕球後の体勢、共通プレー時刻。
- `engine006-tactics.js` / `engine-tactics.js`: 現在情報による候補評価、盗塁時計、内野送球・併殺。
- `engine006-replay.js` / `match-replay.js`: 解決に使った時刻と軌道を表示する。

盗塁では投手モーション、走者スタートと加速、投球到達、捕手の持ち替え、送球、カバー、タッチ、走者到達を同じ時計で比較する。捕手捕球後に走者を新規スタートさせない。三振・四球・第3アウトによる中断も扱う。

守備は PRIMARY FIELDING / SECONDARY CHASE / BACKUP / CUTOFF / BASE COVER / HOLD をプレー途中で切り替える。捕球可能な副追球者だけが追い、内野手の外野への永久追尾を制限する。捕球担当以外はベース・中継・バックアップへ動く。捕球後の定位置リセットはプレー終了後まで行わない。返球位置は打球・送球先・カバーから求め、表示だけ受け手を差し替えない。

走者はボール、捕球可能性、返球の状況を途中で再評価する。予定された単打・二塁打・三塁打に従って走らせない。能力は初動、経路、速度、捕球、体勢回復、送球時間へ作用する。FCは他走者を狙った履歴がある場合の分類であり、無走者で一塁送球しなかっただけでは付けない。エラーはミスと失われたアウト機会・追加進塁の履歴から判定する。

完全な物理・公式記録シミュレーターではない。球場表示の遠近圧縮、離散時間の判断、簡略化した捕球確率を使う。バントも同じ打球・守備経路へ接続したが、今後のバント専用の守備配置拡張は残る。

## CUSTOM MATCH TEST の使い方

ホームまたはエンジン/LAB画面から `CUSTOM MATCH TEST` を開く。

1. A/Bそれぞれの一括ランクを選ぶ。既存表示の正式ランク範囲内の代表値を使用する。
2. 必要な能力だけ数値を変更する。ミート、パワー、走力、肩力、守備力、捕球、球速、制球、スタミナと既存球種・変化量を設定できる。
3. 盗塁○/なし/×、バント○/なし/×を選ぶ。全選手への一括設定で、公式選手DBは変更しない。
4. 1 / 10 / 50 / 100 / 500 / 1000試合を選択して実行する。A/Bのホーム・ビジターは交互に入れ替える。
5. 集計JSONを保存する。中止時にも完了分を残す。
6. 代表場面の「LAB」を開く。または後日JSONを読み込み、同じ場面を開く。

`custom-match-worker.js` は10試合単位（500試合以上は25試合単位）で処理し、バッチの間にUIへ進捗を返す。全イベント配列は保持せず、1イベントずつ集計する。

## JSONの単位と保存上限

形式: `SL_WORLD_CUSTOM_MATCH`、schemaVersion: 1。

- 平均値は CUSTOM A の1試合あたり。得点・失点・安打・本塁打・盗塁企図/成功・犠打・H&R・スクイズ・併殺を保存。
- 戦術選択集計は両チームの投球ごとの判断回数。打席数とは異なり、継続するバント判断等も含む。
- 候補には合法性、重み、正規化した選択比率、理由・入力を保存。重みは「その戦術が成功する確率」ではない。
- 成功率は解決した行動を分母とする。盗塁開始後に三振等で中断した行動と、公式盗塁の成立は区別してログを見る。
- イニング・点差・アウト・走者配置別の戦術回数、候補の生成/合法/正の重み回数を保存。
- 全試合の小さなサマリーを保存。詳細は最大8試合、各最大6場面。全体の代表場面最大32件、異常候補最大40件。
- 最大/最小盗塁企図、最高/最低得点、無作為抽出、レアプレーの場面を上限内で保持する。完全な全試合プレーログではない。
- 場面には打者・次打者・走者・投手・捕手の能力、スコア、カウント、アウト、走者、乱数状態、選択と結果を保存する。

アプリ内の異常検知は走者重複、アウト数、無走者FC、盗塁時刻、捕球前送球、判定時刻、受け手不在など。異常件数0でも野球としての自然さを保証するものではない。

## CUSTOM MATCH → LAB

保存場面をLABへ渡すと、チームと試合状態・乱数状態を復元する。「保存場面の1投を再現」は保存結果と照合し、プレーを時刻スライダーで確認できる。同じエンジン版を使用すること。エンジン版が異なるJSONの厳密再現は拒否する。

高N検証では同じ状況に別の試行seedを与える。完全再現と確率検証を混同しない。LABで走者能力などを変更すれば比較条件へ反映される。1000/2000/5000回などの大量観測はアプリで実施し、JSONから必要な修正を絞り込む。

## 回帰確認

実行コマンド:

```text
node engine-regression.test.cjs
node engine-regression.test.cjs --quick
node round2-regression.test.cjs
node engine-lab-regression.test.cjs
node engine-lab-roads.test.cjs
node serve-engine-lab.cjs
node custom-match.browser.test.cjs
node custom-match.scene.browser.test.cjs
```

ブラウザーテストにはPlaywrightとEdgeが必要。LABの通常回帰は小規模で、明示的な `--stress` 時のみ大量試行する。過去の固定結果を注入するテストは新しい契約に反するため、`engine-regression.test.cjs` は過程ベースの回帰へ差し替えた。元のテストはGit履歴に残る。

過程回帰では盗塁の並行時刻・能力感度、連続打球、未来ラベル非依存、2B頭上の追球と役割変更、ベースカバー、中継、捕球後移動、内野併殺と第3アウト、履歴による記録、ALL C/G/S・B対E、100試合集計のサイズ、全保存場面のLAB再現を検証する。

ブラウザー回帰では100試合のWorker実行、1能力のみの変更、JSON保存/再読み込み、LABの1投一致、スマートフォン幅、1000試合の途中中止、公式DB不変、ページ例外なしを検証する。

## 確認結果と残る検証

今回の最終過程回帰は732イベントを監査し、ALL C同士の100試合を完了。JSONは542,175 bytes（約530 KiB）、代表場面32件、詳細6試合だった。全ログを試合数に比例して保持する構造ではない。

**得点・長打分布は調整完了としていない。** この固定seedの100試合ではAの平均得点0.75、平均安打5.29、平均本塁打0.01と低い。新しい過程から得られた観測値として保存しており、目標成績を先に与えて結果を補正していない。今後CUSTOM MATCH/LABで打球・捕球範囲・走塁判断を個別に観測する必要がある。

追加回帰も通過: 005/006各4,067イベント・各12実選手試合（所有者、連続軌道、FC第3アウト、中継、戦術ゲート）、LAB小規模818イベント、戦術合法性・選手交代、72項目の能力プリセットと正式ランクの一致。

人による実機での連続試合観戦、スマートフォン実機/PWA更新、500/1000試合の完走、全球団・全特殊能力の網羅、長打・得点・戦術頻度の妥当性は未確認。1000試合は途中中止経路のみ確認。高Nの旧SOL試験は今回の回帰対象として完走していない。公式記録の全例外、完全な自責点再構成、バント専用守備の完成も今回の確認範囲に含めない。
