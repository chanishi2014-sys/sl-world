# 戦術AI・守備判断 α1

## 実装と設計

- ENGINE VERSION: `test-match-005-tactics-alpha1`。定義は引き続き `engine-test.html` の `CONFIG.version` のみ。
- `engine-tactics.js` に共通判断を集約。`CONFIG.tactics.enabled` で有効化。監督別の思想は未導入。
- 判断は seed 由来の独立した `tacticalRng` で選択。投球・打撃の既存 RNG 関数と確率表は変更しない。
- ENGINE が結果・アウト・進塁・得点・記録を確定し、EVENT を Viewer が再生する。Viewer は判定しない。

## 守備判断 / HOLD BALL

原因は `engine-fielding.js` の EVENT 生成で、安打後に二塁への返球を無条件に追加していたこと。
到達済みの安打イベントでは各塁のアウト見込みを 0 とし、HOLD BALL を記録、送球イベントを生成しない。

ゴロ・バントの処理では、打者走者と進塁予定走者を各塁の候補に対応付ける。走者不在の塁は除外する。
捕球までの時間・送球距離・肩・走者走力による到達時間差からアウト見込みを算出。
捕球能力・距離による送球ミスのリスク、失敗時の損害、点差・残りイニング・アウト数も評価する。
到達が明らかに遅い候補を除外し、見込みとリスクを比較した重みで選択する。
合理的な候補がない場合は必ず HOLD。候補がある場合も小さい HOLD の重みを残す。
選択後に捕球・送球ミスとアウト成功を別途判定するので、送球を選んでもアウトは保証されない。

## 攻撃判断

- 強攻を常に選択肢に残す。
- バントは三塁走者なし・走者一塁または二塁・2アウト未満・2ストライク未満で検討。
  終盤接戦、弱めの打者、後続打者の打力が高い状況で重みが増す。
  大差や強打者・高長打力打者では選ばない。初球に作戦を選び、2ストライクで強攻へ戻す。
- バント意図と実行結果は独立。見送りボール、ファウル、空振り、小飛球、先行走者アウト、
  野選で全員セーフ、犠打成功、併殺、エラーを扱う。成功は内部成績 SH に記録し AB を増やさない。
  2ストライク後のバントファウルも三振として処理可能。今回の AI は通常その前に作戦解除する。
- 盗塁は進塁先が空いていることを必須条件にする。成功の価値と失敗の損失を比較し、走力・
  アウト数・イニング・点差・捕手の肩・投手の既存制球／球速を参照する。
  既存 DB の盗塁○／×、積極盗塁／慎重盗塁、走塁○／×、クイック○、盗塁阻止○も判断に使用。
  新しい能力項目は追加しない。数値のクイック能力は存在しないため捏造しない。
- 特性は判断の重み・慎重な見積もりだけに使用し、従来の盗塁実行成功率式は維持する。
  同じ投球の間に盗塁を連続実行しない。盗塁もバントも回数の最低保証はない。

## EVENT / DEBUG

既存 EVENT は維持し、以下を追加する。

- `tactics.intent`, `tactics.decision`, `tactics.execution`, `tactics.outcome`
- `tactics.offense` / `tactics.defense`: 候補、重み、理由、状況、使用入力
- `defenseDecision`: 各塁の `outChance`, `ballETA`, `runnerETA`, `distance`, `errorRisk`, `catching`, `damage`
- `bunt`, `buntAttempt`, `buntQuality`, `buntPop`, `sacrificeBunt`
- 送球・走者・タッチは既存 `transfers`, `coverage`, `actions` に統合

例（説明用抜粋）:

```text
Decision: HOLD BALL
Reason:
- no reachable out; unnecessary throw risk
- 1B out chance: 0.00
- 2B out chance: 0.00

Decision: SAC BUNT
Reason:
- inning: 8; remaining: 2
- score difference: 0; outs: 0
- one-run value: 1
```

DEBUG は初期 OFF。ON にしたときだけフィールド下に理由を表示する。
停止中も直前の描画から即時更新できる。理由テキストは折り返す。

## Viewer

横にバットを出すバントの構え、弱い低バウンドのゴロを追加。
ENGINE が指定した P/C/1B/3B の主担当野手が処理し、既存の補助反応・ベースカバー・送球と
走者の進塁アニメーションにつなぐ。盗塁の捕手送球→カバー捕球→タッチは既存経路を再利用。
HOLD に送球アニメーションは生成しない。ハイライトにもバントを追加。

## 検証

実行コマンド（ブラウザ用には Playwright の NODE_PATH が必要）:

```text
node engine-regression.test.cjs
node tactics-regression.test.cjs
node lineup-display.test.cjs
node match-viewer.browser.test.cjs
```

- 従来モード: 150試合 + 40件のモード再実行、2,400ゴロ状況、34,143イベントのタイムライン。
  30 seed の保存済み結果ハッシュ一致。新規 SH フィールドだけ旧形式との比較対象から除外。
- 新戦術: 100試合 + 40件のモード再実行、25,165イベント。
  FULL/MINI/HIGHLIGHT/SKIP の全状態・R/H/E・得点・走者一致、全試合完走、例外なし。
  バント0回 56試合、盗塁0回 88試合、SB 10、CS 3（合成ロスター固定100 seed）。
- バント専用240打席: 犠打136、野選36、小飛球アウト27、四球15、三振15、併殺8、エラー3。
  ファウル85球・空振り51球も確認。成功時の AB/SH と進塁を検証。
  速い走者と遅い走者を交互に使い、合理的な守備候補が異なる状況を検証。
- 指定状況: 到達済み内野安打の HOLD、間に合わないゴロの HOLD、対象走者のない塁の除外、
  同じ送球判断からアウト／セーフ両方、占有塁への盗塁禁止、同一投球内の連続盗塁禁止、
  終盤接戦と打力によるバント評価差、俊足と鈍足の盗塁評価差。
- 実ブラウザ: 2 seed × 4モード = 8試合。9種類の実生成プレーを499フレーム描画。
  バント、盗塁刺殺、ゴロ、ライナー、安打、併殺、タッチアップ、エラー、攻守交代を確認。
  390px幅、DEBUG ON/OFF、undefined/null文字列が出ないこと、Storage書込APIが呼ばれないことを検証。
- 能力表示: 776の有効整数値・境界・未設定値・両チーム／二刀流の既存テスト通過。
- 選手入力オブジェクトとチーム原本が試合後も不変であることを比較。

## 変更ファイル

`engine-tactics.js`, `engine-fielding.js`, `engine-test.html`, `match-replay.js`, `match-viewer.js`,
`service-worker.js`, `tactics-regression.test.cjs`, `engine-regression.test.cjs`,
`match-viewer.browser.test.cjs`, 本書。

## 影響・制限・今後

選手DB・player-editor・localStorage形式・能力値・能力ランク・既存打撃／投球確率表は変更しない。
戦術によりプレー選択と守備の成否が変わるため、新戦術ONのスコアは旧版と同じとは限らない。
従来設定の30 seed一致を、確率表と旧経路の回帰確認に使用する。

α1 は到達時間・価値・リスクの簡易推定で、実測物理や本格的な得点期待値モデルではない。
安打は既存処理で走者到達が確定しているため HOLD とし、追加アウトを後付けしない。
送球後逸による連続追加進塁、バント安打専用判定、スクイズ、本盗、重盗、交代AIは未対応。
SH は内部記録で、個人成績表への専用列追加は今回の範囲外。

今後は候補評価への manager strategy/personality、監督サインと走者判断の分離、詳細な
送球ミス・走塁データを加えられる。現時点の判断は共通ポリシー shared-alpha1 に集約する。

牽制誘発のリード、盗塁のふり、構えだけの揺さぶり、フェイクバント、投手への心理的圧力、
癖を知っていることを隠す行動、監督心理戦、意図的ミスリード、代走の心理効果、
高度なエンドランは将来候補として記録するだけで実装しない。
