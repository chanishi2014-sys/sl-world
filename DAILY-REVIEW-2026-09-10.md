# SL WORLD 2026-09-10 制作報告

## 1. 調査した原因

| 項目 | 原因・判明事項 |
|---|---|
| FC分類 | 実戦・006とも `applyPitch → geometry/buntGeometry → field → groundPlay → defenseDecision → 捕球/送球エラー → outChance → advanceGround → 記録分類`。先行走者を狙ったかどうかにかかわらず、最後の分岐がFCを返していた。一塁送球が間に合わないケースと送球見送りまでFCになっていた。006にはセーフティバントなら先行走者への送球失敗も安打にする逆方向の例外もあった。 |
| 外野返球 | HOMEリンクは `engine-test.html`。LABは006の別ファイル群を使用。実戦 `engine-fielding.js` の安打後annotationは `raw=[]` のまま、006だけにある返球生成が届いていなかった。再生側 `animationPlan` は最終transfer完了後にresultAt/endを置くため、今回確認した原因は転送イベントの欠落。 |
| SAC_BUNT | 006の `resolveBunt` は特性をcontactの既存±0.10、buntQualityの既存±0.12へ反映。品質は打球長・捕球時間・送球先候補・到達差に届く。上書きやclampによる差の消失は確認されなかった。LABの成功集計は `e.sacrificeBunt` のみで、内野安打による進塁は成功に含まれない。良い品質で打者が生きるほど、犠打記録率と実行品質が一致しなくなる。 |
| STEAL | `stealOption` の予測式と `SL_FIELDING.steal` の実行式に盗塁○/×が含まれない。選択weightには1.15/0.7がある。`TACTICS-ALPHA1.md` に「特性は判断の重み・慎重な見積もりだけに使用し、従来の盗塁実行成功率式は維持」と明記されており、配線の脱落ではなく現行仕様。成功効果量の既存定義はない。 |
| SQUEEZE | 選択weightは `.25 * oneRunValue * meet`。バント特性・三塁走者走力・パワー・成功見込みは選択式に入らない。ミートGの正規化値0ならweightも0。成功処理にはバント品質と走者到達時間が別経路で作用する。意図された重みの明確な誤配線は確認できない。 |
| LAB固定条件 | `players` の個別コピーがトップレベルのプリセット値より優先される既存仕様に対し、trait/grade metadataが追従しない。プリセット切替はconfigを作り直しており、前回状態が必ず残る経路は確認できない。複数走者の基準値を共有したままmetadataを更新すると他塁へ波及する危険もある。 |
| metadata/display | compareは能力値だけを変更し、scenarioのgradeとTEST選手名が基準条件のまま残る。 |
| 投球数比較 | 比較条件・件数計算ともaxes.fatigue.valuesの0/80/140固定。単一条件のCustom入力とは別。 |
| 所属選手一覧 | 名前だけのボタン。詳細は既存 `SL_ABILITY_DISPLAY.describeAbility` とランクCSSを使用しているため、そのまま一覧にも再利用できる。 |

## 2. 実際に修正した内容

- **FC**：エラー判定を先に維持したうえで、先行走者を狙っていない一塁セーフ／送球見送りは内野安打。打者H・投手被安打・チーム安打を各1回更新。先行走者狙いは成功/失敗ともFCを維持。006のセーフティバント例外もこの分類に統一。
- **実戦返球**：実戦の守備判断へSECURE_RETURNを追加し、annotationから二塁へのreturn transferを生成。外野単打・二塁打・三塁打に加え、走者が残る外野フライ・エラーの無送球経路も対象。乱数や追加のアウト判定は増やしていない。006既存返球処理は変更なし。
- **LAB入力と表示**：normalizeScenarioで既定選手・占有塁の走者を先にコピーして固定し、実際の選手コピーからtrait/grade metadataを生成。比較軸以外の能力を固定。生成TEST選手の名前は実能力値から生成し、登録選手名は保持。元scenarioを変更しない。
- **入力優先順位**：既存どおり個別選手コピーが優先。例えばtop-level noneに対して個別選手がバント○なら、保存条件のtop-levelを○に揃える。○を意図せずnoneへ書き換える処理ではない。UIで「なし」を選んだ個別選手は比較全条件でもnoneを維持する。
- **投球数比較**：詳細設定に複数Custom値入力。例 `90 / 100 / 110`。2〜20個の異なる非負整数を受け付け、空欄なら0/80/140。カンマ・空白・スラッシュ区切り。プレビュー、2軸matrix、試行件数、Worker、保存scenarioで同じ値列を使用。
- **一覧UI**：ミート→パワー→走力→肩力→守備力→捕球の6列。上段ランク、直下の小さい実数値。表示上の能力名ラベルはなし。読み上げ用ラベルは付与。詳細と同じ登録データ・ランク判定・色クラスを利用。

## 3. 変更した主要ファイル

- `engine-tactics.js`：実戦FC分類、返球判断。
- `engine-fielding.js`：実戦返球イベント生成。
- `engine006-tactics.js`：006 FC分類。
- `engine-lab-core.js`：条件正規化、metadata/TEST名、可変投球数比較。
- `engine-lab-ui.js` / `engine-lab-worker.js`：複数値入力・実行件数の伝達。
- `teams-view.js` / `teams-view.css`：所属選手一覧の能力表示。
- `tactics-regression.test.cjs`：一塁送球セーフの旧FC期待値をsingleへ更新。
- `daily-2026-09-10.test.cjs` / `daily-2026-09-10-traits.test.cjs` / `daily-2026-09-10.browser.test.cjs`：今回の分類・返球・LAB・特性追跡・ブラウザ検証。

## 4. 実行したテスト

- `node engine-regression.test.cjs`
- `node tactics-regression.test.cjs`
- `node engine-lab-regression.test.cjs`
- `node lineup-display.test.cjs`
- `node daily-2026-09-10.test.cjs`
- `node daily-2026-09-10-traits.test.cjs`
- `node daily-2026-09-10.browser.test.cjs`（bundled Playwright、headless Edge。HTTP時はSL_LAB_URLを指定しWorkerも実行）
- 履歴固定テスト：`engine-lab-sol.test.cjs`、`engine-lab-roads.test.cjs`、`engine-lab-ui.test.cjs`、`engine-lab-matrix.test.cjs`
- `git diff --check`

## 5. テスト結果

**挙動テスト通過**：実戦150試合・40モード比較・2,400ゴロ状況・34,143イベント、戦術100試合・40モード比較・240バント打席、006 30試合・12モード比較・900シナリオ試行・3,000感度試行・10,000bulk試行。能力ランク全776整数値と境界を確認。

追加テスト：両エンジン各300ゴロで安打38・FC110・エラー6・併殺44・ゴロアウト102を確認。両経路計30外野返球ケースでイベント生成とtransfer完了後の結果表示を確認。固定trait、複数走者の固定能力、matrix、bench metadata、JSON export/import、SOL metrics、Custom不正値拒否を確認。

ブラウザ：360px幅で一覧のランク・数値・能力名非表示・行内overflowなしを確認。画面画像も確認。90/100/110の比較プレビューとWorker 3条件×2試行が通過。pageerrorなし。検証にはブラウザ専用の合成ロスターを使用し、製品にダミー選手は追加していない。

バント診断（作戦選択を介さず各5,000打席を強制バント、初球品質を採取）：

| 特性 | 初球inZone | 初球接触 | 平均品質 | 犠打 | 内野安打 | 三振 |
|---|---:|---:|---:|---:|---:|---:|
| ○ | 3074 | 2571 | 0.6210 | 2499 | 1119 | 21 |
| なし | 3074 | 2252 | 0.5004 | 2655 | 878 | 65 |
| × | 3074 | 1984 | 0.3805 | 2702 | 719 | 181 |

これはユーザーの初手選択後の条件付きLAB成功率と同一母集団ではない。特性の配線と、犠打率だけでは成功品質を評価できない点を切り分けるための診断。

**履歴固定4本は失敗**：SOLは旧seedトレースハッシュ、roadsは比較軸以外の名前/gradeも完全一致する旧期待、UI/matrixは変更禁止ファイルの旧ハッシュで停止。今回の許可済みFC・metadata・実戦返球変更と両立しない期待がある。これらの固定値は一括更新しておらず、全既存テスト成功とは扱わない。追加テストで現在のexport/importと条件固定を検証したが、履歴固定テストの後続assertすべてを代替したとは主張しない。

## 6. 修正せず調査のみとした項目

- **SAC_BUNT成功式**：特性は既に作用し、clampで消えていない。犠打だけを成功とする計測定義と、打者も生きる実行結果の違いが確認された。成功確率や集計定義を任意に変更しない。
- **STEAL成功式**：選択のみ特性を使う明文化された設計。成功率へ作用させる効果量・方式は未定義であり、選択倍率の流用や任意加算を行わない。
- **SQUEEZE選択**：明示されたmeet依存式で、配線漏れの証拠なし。
- **PINCH_HIT**：prepare関数のソースがHEADと同一であることをassertし、変更していない。

## 7. 未解決事項・追加検証

- 盗塁○/×を成功率にも反映する仕様は未実装のまま。効果を与える場所（スタート、到達時間、確率等）と根拠のある強度を決める必要がある。
- バントの「成功」が犠打記録か、走者進塁を含む作戦達成かを明確化する余地がある。今回は既存指標を維持。
- 外野返球は、現行エンジンが進塁結果を確定した後のイベント生成で補完。追加進塁を継続判断するリアルタイム守備状態機械は新設していない。走者が実際に進塁可能な状態で終了する個別seedは、実戦ログで追加確認が必要。
- LABとHOME実戦は別エンジンのまま。今回は両方のFCと実戦返球を修正したが、バント等の機能差を一括統合していない。
- 過去の保存JSON自体は書き換えない。旧seedを新コードで再計算すると、FC/安打の記録やTEST表示名が変わる。旧snapshotとの完全一致を前提にしない。
- 4本の履歴固定テストは、今回変更した契約を反映した期待値整理が残る。
- 本報告はローカルコード・自動テスト・headlessブラウザでの確認。ユーザー環境のSL WORLD上で最終修正確認済みとは扱わない。

## 8. 翌日ユーザー側で確認する回帰項目

1. HOME→実戦2Dで一塁送球セーフが内野安打になり、打者/投手/チーム安打が一致する。
2. 先行走者を狙った全員セーフ・封殺はFC、明確な捕球/送球ミスはエラーのまま。
3. 外野単打・二塁打・三塁打後に二塁/中継への返球が見える。FULL/MINIと最終打席も確認。未進塁余地がある不自然な終了はseedとJSONを保存。
4. LABのSECURE_RETURN、H&R、スクイズ成功、送りバント/盗塁の初手選択を再比較。
5. バント○/なし/×は犠打率に加えて内野安打・三振・進塁結果も確認。盗塁成功率の特性差は今回未追加。
6. プリセット切替→打者特性なし→ミート比較・matrixで、各条件のspecials/effectiveInputs/scenarioが一致し、他塁走者の固定走力が変わらない。
7. bench/next/batter比較のgradeと実能力・TEST名、JSONの条件表示を確認。
8. 球数90/100/110を比較軸として実行・保存・再読込し、3条件と試行件数が一致する。空欄の0/80/140も確認。
9. SL/TL所属選手一覧をスマホで確認。6能力の順序、数値、ランク色、詳細との一致、長い選手名を確認。
10. 6回/7回以降の代打挙動が従来どおりであることを確認。
